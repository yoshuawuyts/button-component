{
  "name": "button-component",
  "version": "1.0.2",
  "description": "Create an extendable customElement button",
  "main": "index.js",
  "scripts": {
    "start": "cd example && open http://localhost:9966 && wzrd index.js",
    "test": "standard && NODE_ENV=test browserify test.js | tap-closer | smokestack --browser chrome",
    "test-cov": "standard && NODE_ENV=test browserify -t browserify-istanbul test.js | tap-closer | smokestack --browser chrome"
  },
  "repository": "yoshuawuyts/button-component",
  "keywords": [
    "webcomponents",
    "webcomponent",
    "component",
    "browser",
    "button"
  ],
  "license": "MIT",
  "dependencies": {
    "custom-element": "^2.0.0",
    "tachyons-buttons": "^1.0.2"
  },
  "devDependencies": {
    "browserify": "^9.0.8",
    "browserify-istanbul": "^0.2.1",
    "es6-symbol": "^2.0.1",
    "smokestack": "^3.2.2",
    "standard": "^3.3.2",
    "tap-closer": "^1.0.0",
    "tape": "^4.0.0",
    "webcomponents.js": "^0.6.0",
    "wzrd": "^1.2.1"
  },
  "files": [
    "LICENSE",
    "index.js",
    "README.md"
  ]
}
